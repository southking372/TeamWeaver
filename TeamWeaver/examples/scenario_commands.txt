# ============================================================================
# LM-MIQP Demo - All Task Scenario Commands
# ============================================================================
# Usage: Copy the corresponding command to your terminal and run it
# Note: You may need to adjust parameters such as model name for your environment
# ============================================================================

# ============================================================================
# Scenario a) Heterogeneous Task Execution
# ============================================================================
# Tests the system's ability to coordinate different robots to fulfill user requests
# Evaluation: task allocation efficiency, capability utilization, task completion
# ============================================================================

# Version 1: Basic exploration and collection task
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="Explore Room1 and Room2 to find all objects then collect them and place them on the tables in Room3"

# Version 2: Detailed multi-step task
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="Send agents to explore Room1 and Room2 separately to search for objects. Once objects are found navigate to those rooms pick up each object and deliver them to Room3 placing them on the marked positions on the tables"

# Version 3: Explicit room exploration assignment
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="Agent 1 should explore Room1. Agent 2 should explore Room2. After finding objects Agent 3 should collect them and place on tables in Room3"


# ============================================================================
# Scenario b) Robust under Error-Prone Conditions
# ============================================================================
# Tests the system's replanning ability when the environment changes
# Evaluation: task stability, replanning count, task completion
# ============================================================================

# Version 1: Sequential exploration and collection (simulated obstacle scenario)
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="First explore Room1 to find objects. If objects are found immediately collect and deliver them to Room3. Then explore Room2 and collect any objects found there also delivering them to Room3"

# Version 2: Path planning with obstacles in mind
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="Explore accessible rooms avoiding obstacles in the hallway. Collect all objects found in Room1 first deliver them to Room3 then explore Room2 and collect objects there"

# Version 3: Priority task assignment
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="Prioritize exploring Room1. Collect objects from Room1 and deliver to Room3. After that explore Room2 and collect remaining objects"


# ============================================================================
# Scenario c) Human-Initiated Collaboration
# ============================================================================
# Tests the system's ability to adapt to changing human intent
# Evaluation: semantic transparency, execution transparency, task adaptability
# ============================================================================

# Step 1: Initial instruction - collect all objects
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="Put all the objects on the tables in Room3"

# Step 2: Updated instruction - only one object needed (simulates mid-task intent change)
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="I only need one object now. Place just one object on the table in Room3 and return the rest to their original locations"

# Version 2: More natural conversational instruction
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="Hey please collect all objects and organize them on the tables in Room3"


# ============================================================================
# Other Test Scenarios
# ============================================================================

# Scenario 1: Pure exploration task
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="Explore all three rooms and report what objects are found in each room"

# Scenario 2: Single-step operation task
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="Navigate to Room1 pick up the red object then navigate to Room3 and place it on table_3a"

# Scenario 3: Complex multi-step collaboration
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="Organize the environment. Collect objects from Room1 move furniture in Room2 and arrange items on tables in Room3"

# Scenario 4: Simple collection task
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="Collect the red object from Room1 and explore all rooms"

# Scenario 5: Multi-room navigation and manipulation
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="Navigate through all rooms identify all objects and create a plan to organize them efficiently"

# Scenario 6: Specified target locations
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="Find all objects in Room1 and Room2 then move them to the tables in Room3"

# Scenario 7: Exploration first
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="First explore Room1 then Room2 then Room3. Report findings and then collect objects"

# Scenario 8: Collaborative division of labor
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  instruction="Divide tasks. One agent explores Room1 another explores Room2 and the third agent collects and organizes found objects"


# ============================================================================
# Command Examples with Model Parameters (if you need to specify a particular model)
# ============================================================================

# Using moonshot model
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  llm.model=moonshot-v1-32k \
  instruction="Explore Room1 and Room2 to find all objects then collect them and place them on the tables in Room3"

# Using another model (adjust according to your API configuration)
python -m habitat_llm.examples.simple_lm_miqp_demo_hydra \
  --config-name examples/simple_lm_miqp_demo \
  llm.model=your-model-name \
  instruction="Put all the objects on the tables in Room3"


# ============================================================================
# Metrics Reference
# ============================================================================
# The following metrics are output after running:
#
# 【a) Task Execution Metrics】
#   - SR (Success Rate): Success rate
#   - PC (Percent Complete): Percent complete
#   - SS (Simulation Steps): Simulation time steps
#   - PCy (Planning Cycles): Planning cycle count
#
# 【b) Task Allocation Metrics】
#   - CU (Capability Utilization): Capability utilization
#   - TSt (Task Stability): Task stability
#
# 【c) Transparency Metrics】
#   - FA (Factual Accuracy): Factual accuracy
#   - STL (Semantic Transparency): Semantic transparency
#   - STE (Execution Transparency): Execution transparency
#
# 【Hallucination Evaluation Metrics】
#   - Object Existence: Object existence
#   - Action Coherence: Action coherence
#   - Constraint Satisfaction: Constraint satisfaction
#   - Overall Truthfulness: Overall truthfulness
#   - Overall Coherence: Overall coherence
#   - Hallucination Score: Hallucination score (lower is better)
#   - Reliability Score: Reliability score (higher is better)
# ============================================================================
